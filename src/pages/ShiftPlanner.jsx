import React from 'react';
import ShiftPlanner from '../components/ShiftPlanner';

const ShiftPlannerPage = ({ user }) => {
  return (
    <ShiftPlanner user={user} />
  );
};

export default ShiftPlannerPage;
